/**
 * Store barrel exports
 * Provides clean import paths for store hooks and types
 *
 * Usage:
 * import { useAppStore, type AppState } from '@/stores';
 */

export { useAppStore, type AppState } from './app-store';
